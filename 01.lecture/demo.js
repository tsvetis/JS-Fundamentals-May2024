// let, const
// don't use var! hoisting

// declaration -> creates a variable in memory
let a = 5; // definition (assignment) -> assignes value to the variable inside memory
const b = -15; // declaration + definition
console.log(b === a);
