function solve(firstName, secondName, symbol) {
  //   console.log(firstName + symbol + secondName);
  console.log(`${firstName}${symbol}${secondName}`);
}

solve("John", "Smith", "->");
solve("Jan", "White", "<->");
